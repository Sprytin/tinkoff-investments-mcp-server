## Tinkoff Invest API and MCP server configuration (stdio transport)
## Документация: https://docs.spring.io/spring-ai/reference/api/mcp/mcp-server-boot-starter-docs.html
## Профили инструментов:
##   READ:    SPRING_PROFILES_ACTIVE=read-mcp
##   TRADE:   SPRING_PROFILES_ACTIVE=trade-mcp
##   SANDBOX: SPRING_PROFILES_ACTIVE=sandbox-mcp
## Чтобы загрузить все инструменты сразу, активируйте все 3 профиля (см. ниже).

spring:
  application:
    name: rich
  ai:
    mcp:
      server:
        name: rich-mcp-server       # Отображаемое имя сервера
        version: 1.0.0              # Версия сервера
        type: SYNC                  # Тип сервера: SYNC для stdio
        transport: STDIO            # STDIO транспорт вместо HTTP
        instructions: "This server exposes Tinkoff Invest API tools for read, trade and sandbox operations."
        capabilities:
          tool: true
          resource: true
          prompt: true
          completion: true
  profiles:
    active: read-mcp,trade-mcp,sandbox-mcp  # По умолчанию загружаем все инструменты (эквивалент "all")

tinkoff:
  token: "ВАШ_ТОКЕН_ЗДЕСЬ"        # 🔴 ОБЯЗАТЕЛЬНО замените на ваш реальный токен
  app-name: rich-mcp-server
  target: invest-public-api.tinkoff.ru:443
